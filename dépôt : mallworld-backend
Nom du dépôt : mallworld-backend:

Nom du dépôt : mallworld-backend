{
  "name": "mallworld-backend",
  "version": "1.0.0",
  "description": "Backend de MallWorld prêt pour Vercel",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "nodemon": "^2.0.22"
  }
}const express = require("express");
const app = express();
const PORT = process.env.PORT || 3000;

// Middleware pour JSON
app.use(express.json());

// Endpoint de santé
app.get("/health", (req, res) => {
  res.status(200).json({ status: "ok", timestamp: Date.now() });
});

// Exemple endpoint API
app.get("/api/v1/products", (req, res) => {
  res.json([
    { id: 1, name: "Produit A", price: 10 },
    { id: 2, name: "Produit B", price: 20 }
  ]);
});

// Exemple endpoint API
app.get("/api/v1/malls", (req, res) => {
  res.json([
    { id: 1, name: "Mall Central" },
    { id: 2, name: "Mall West" }
  ]);
});

// Démarrage du serveur
app.listen(PORT, () => {
  console.log(`🚀 MallWorld backend running on port ${PORT}`);
});node_modules
.env
# Variables d'environnement
PORT=3000
JWT_SECRET=change_me
DATABASE_URL=postgres://user:pass@localhost:5432/mallworld
REDIS_URL=redis://:password@localhost:6379